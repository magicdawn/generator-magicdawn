import _ from 'lodash'
import SetupGenerator, {ISetupAction} from '../'

export default {
  label: 'electron',
  desc: 'Electron stuff',
  fn,
} as ISetupAction

async function fn(this: SetupGenerator) {
  //
}
